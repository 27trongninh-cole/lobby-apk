package com.ninfinity.aovmapmod.ui.theme

import androidx.compose.ui.graphics.Color

// Nền chính — đen tuyền như ảnh mẫu
val BackgroundBlack = Color(0xFF000000)

// Card / surface — xám đậm, bo góc lớn
val SurfaceDark = Color(0xFF1C1C1E)
val SurfaceDarkElevated = Color(0xFF2C2C2E)

// Accent — tím nhạt (nút "Tệp mới", icon chính) lấy cảm hứng từ ảnh mẫu
val AccentPurple = Color(0xFFB39DFF)
val AccentPurpleDim = Color(0xFF6E5B9E)

// Accent phụ — hồng nhạt (icon Mở tệp)
val AccentPink = Color(0xFFF4A9C7)

// Text
val TextPrimary = Color(0xFFF2F2F7)
val TextSecondary = Color(0xFF9A9A9E)

// Trạng thái mod (giữ tinh thần app hiện tại: cam = đã sửa, xanh = đã rebuild)
val StatusModified = Color(0xFFFFB454)
val StatusRebuilt = Color(0xFF4CD97B)
val StatusError = Color(0xFFFF6B6B)
